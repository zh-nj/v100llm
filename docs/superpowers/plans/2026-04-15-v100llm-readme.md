# v100llm README Refresh Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 补一份新的 `README.md`，让 GitHub 首页和打包元数据都与当前 `v100llm (vLLM 0.19.0 for V100/SM70)` 定位一致。

**Architecture:** README 采用“发布型首页 + 工程验证入口”结构：先说明项目定位和安装方式，再列已验证模型、与 upstream 的差异、限制和文档入口。只修改仓库首页文档，不改运行时代码或包名。

**Tech Stack:** Markdown, setuptools/pyproject metadata, existing verification docs

---

## File Map

- Create: `README.md`
  - 新的双语仓库首页
  - 说明仓库定位、源码安装路径、已验证模型、文档入口和版本口径
- Create: `docs/superpowers/plans/2026-04-15-v100llm-readme.md`
  - 记录本次 README 刷新的实施步骤

## Task 1: 编写新的仓库首页 README

**Files:**
- Create: `README.md`
- Test: `pyproject.toml:readme = "README.md"`

- [ ] **Step 1: 编写 README 草稿**

README 需要包含以下章节：

```markdown
# v100llm (vLLM 0.19.0 for V100/SM70)

中文简介

English summary

## Highlights / 项目亮点
## Quick Start / 快速开始
## Validated Models and Paths / 已验证模型与路径
## What Changed vs Upstream vLLM / 与上游差异
## Current Status and Limits / 当前状态与限制
## Docs and Verification / 文档与验证
## Build and Release Notes / 构建与发布说明
## License / 许可证
```

- [ ] **Step 2: 将 Quick Start 固定为源码安装路径**

README 中的安装命令应包含：

```bash
conda create -n v100llm python=3.13 -y
conda activate v100llm
python -m pip install --upgrade pip setuptools wheel
python -m pip install torch torchvision torchaudio --index-url https://download.pytorch.org/whl/cu128
export VLLM_FLASH_ATTN_SRC_DIR=/path/to/flash-attention-v100
python -m pip install -e . --no-build-isolation
python -m vllm.entrypoints.cli.main --version
```

并明确说明：

- 当前对外打包版本为 `0.19.0`
- 仓库品牌名是 `v100llm`
- 安装后的包名和 CLI 仍然是 `vllm`

- [ ] **Step 3: 只写入已验证内容**

README 只列以下已验证模型：

```text
Qwen3.5-27B-AWQ
Qwen3.5-122B-A10B-AWQ-4bit
MiniMax-M2.5-AWQ
MiniMax-M2.7-AWQ-4bit
```

并把详细命令和结果链接到：

```text
docs/upstream-sync/verification-2026-04-13-v0190-worktree-runtime-recovery.md
```

- [ ] **Step 4: 保存 README 并检查 Markdown 内容**

Run:

```bash
sed -n '1,260p' /mnt/data/apps/1Cat-vLLM/.worktrees/vllm-0190-upstream-split/README.md
```

Expected:

- README 结构完整
- 不再引用旧版 `0.0.2`
- 不再引用已删除图片

## Task 2: 验证 README 已重新接入打包元数据

**Files:**
- Modify: none
- Test: `README.md`
- Test: `pyproject.toml`

- [ ] **Step 1: 运行 `setup.py --version` 确认不再提示缺失 README**

Run:

```bash
bash -lc 'source /home/z/anaconda3/etc/profile.d/conda.sh && \
conda activate gptq && \
cd /mnt/data/apps/1Cat-vLLM/.worktrees/vllm-0190-upstream-split && \
unset PYTHONPATH && \
python setup.py --version'
```

Expected:

- 输出 `0.19.0`
- 不再出现 `File .../README.md cannot be found` 的 setuptools warning

- [ ] **Step 2: 运行 `git diff --check` 确认 README 没有格式错误**

Run:

```bash
git -C /mnt/data/apps/1Cat-vLLM/.worktrees/vllm-0190-upstream-split diff --check
```

Expected:

- 无输出

- [ ] **Step 3: 提交 README 刷新**

```bash
git -C /mnt/data/apps/1Cat-vLLM/.worktrees/vllm-0190-upstream-split add \
  README.md \
  docs/superpowers/plans/2026-04-15-v100llm-readme.md
git -C /mnt/data/apps/1Cat-vLLM/.worktrees/vllm-0190-upstream-split commit -m \
  "docs: add v100llm repository readme"
```
