# v0.19.1 + Transformers 5.5.4 + SM70 Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 将 `feature/vllm-0190-upstream-split` 升级到 `vLLM 0.19.1` 基线，并把 `transformers` 切到 `5.5.4`，同时保留已经跑通的 `SM70 / Gemma4 / FA2 hdim512 / compressed-tensors AWQ` 路径。

**Architecture:** 先把版本号和 requirements 固定到目标状态，并用文件级回归测试锁住这些约束；随后补齐 `huggingface_hub 1.x` 的兼容防线，增加本地 `Gemma4` tokenizer / processor 回归；最后只在必要处校正当前 worktree 中已经存在的 `SM70` 运行时代码，并用 `SM70` 单测、真实 `Gemma4` 2-GPU smoke 和 `serve / OpenAI API` 验证收口。

**Tech Stack:** Python 3.13, Conda `gptq`, `uv pip compile`, `pytest`, editable `vllm` install, `Tesla V100 / SM70`, local model `/mnt/data6/models/gemma-4-31B-it-AWQ-4bit`, local upstream mirror `/mnt/data/apps/vllm`

---

## File Map

- Modify: `setup.py`
  - 将固定发布版本从 `0.19.0` 提升到 `0.19.1`
- Modify: `requirements/common.txt`
  - 对齐上游 `v0.19.1` 的 `transformers` 约束
  - 保留当前分支已经引入的 `compressed-tensors == 0.15.0.1`
- Modify: `requirements/test.in`
  - 将测试锁版本从 `transformers==4.57.5` 提升到 `transformers==5.5.4`
  - 保持 `tokenizers==0.22.2`
- Modify: `requirements/test.txt`
  - 使用 `uv pip compile` 从 `requirements/test.in` 重新生成
- Modify: `tests/test_version.py`
  - 把固定版本断言从 `0.19.0` 改成 `0.19.1`
- Create: `tests/test_dependency_versions.py`
  - 直接校验仓库文件中的版本约束和锁版本
- Modify: `vllm/model_executor/model_loader/weight_utils.py`
  - 让 `huggingface_hub 1.x` 下不存在旧常量时也能安全导入
- Modify: `tests/model_executor/test_weight_utils.py`
  - 覆盖 `HF_HUB_ENABLE_HF_TRANSFER` / `HF_XET_HIGH_PERFORMANCE` 常量缺失场景
- Create: `tests/tokenizers_/test_gemma4_local.py`
  - 本地 `Gemma4` fast tokenizer 和 `CachedHfTokenizer` 回归
- Create: `tests/transformers_utils/test_gemma4_local_processor.py`
  - 本地 `Gemma4` processor 回归
- Preserve and verify: `vllm/v1/attention/backends/flash_attn.py`
  - 保留 `SM70` 上 `head_dim=512` 的允许逻辑
- Preserve and verify: `vllm/model_executor/models/config.py`
  - 保留 `Gemma4Config.verify_and_update_config()` 不强设全局 backend 的行为
- Preserve and verify: `vllm/model_executor/models/gemma4.py`
  - 保留 `Gemma4` layer-level mixed attention 选择逻辑
- Preserve and verify: `vllm/model_executor/layers/quantization/compressed_tensors/compressed_tensors.py`
  - 保留 `CompressedTensorsSM70WNA16` 的选择逻辑
- Preserve and verify: `vllm/model_executor/layers/quantization/compressed_tensors/schemes/__init__.py`
  - 保留 `CompressedTensorsSM70WNA16` 导出
- Preserve and verify: `vllm/model_executor/layers/quantization/compressed_tensors/schemes/compressed_tensors_wNa16_sm70.py`
  - 保留 SM70 dense int4 AWQ fallback / prepare / gemm 路径
- Preserve and verify: `tests/v1/attention/test_flash_attn_sm70.py`
- Preserve and verify: `tests/v1/attention/test_gemma4_sm70_mixed_attention.py`
- Preserve and verify: `tests/quantization/test_compressed_tensors_sm70.py`
- Modify: `README.md`
  - 将对外版本说明从 `0.19.0` 提升到 `0.19.1`
  - 增加 `transformers 5.5.4` 的说明
- Modify: `docs/open_source_sm70_flash_attn_install_upgrade_zh.md`
  - 更新推荐环境和升级步骤里的版本口径

## Task 1: 固定版本与依赖基线

**Files:**
- Modify: `setup.py`
- Modify: `requirements/common.txt`
- Modify: `requirements/test.in`
- Modify: `requirements/test.txt`
- Modify: `tests/test_version.py`
- Create: `tests/test_dependency_versions.py`
- Test: `tests/test_version.py`
- Test: `tests/test_dependency_versions.py`

- [ ] **Step 1: 先写失败回归，锁住目标版本和 requirements**

在 `tests/test_version.py` 中把现有版本断言改成：

```python
def test_release_version_matches_v0191():
    assert version.__version__ == "0.19.1"
    assert version.__version_tuple__[:3] == (0, 19, 1)
```

新建 `tests/test_dependency_versions.py`：

```python
# SPDX-License-Identifier: Apache-2.0
# SPDX-FileCopyrightText: Copyright contributors to the vLLM project

from pathlib import Path


ROOT = Path(__file__).resolve().parents[1]


def _read(rel_path: str) -> str:
    return (ROOT / rel_path).read_text(encoding="utf-8")


def test_common_requirements_allow_transformers_55x():
    text = _read("requirements/common.txt")
    assert (
        "transformers >= 4.56.0, != 5.0.*, != 5.1.*, != 5.2.*, "
        "!= 5.3.*, != 5.4.*, != 5.5.0"
    ) in text
    assert "compressed-tensors == 0.15.0.1" in text


def test_test_requirements_pin_transformers_554():
    assert "transformers==5.5.4" in _read("requirements/test.in")
    test_txt = _read("requirements/test.txt")
    assert "transformers==5.5.4" in test_txt
    assert "tokenizers==0.22.2" in test_txt
```

- [ ] **Step 2: 跑失败回归，确认当前基线还停在 `0.19.0 / transformers 4.57.5`**

Run:

```bash
bash -lc 'source /home/z/anaconda3/etc/profile.d/conda.sh && \
conda activate gptq && \
cd /mnt/data/apps/1Cat-vLLM/.worktrees/vllm-0190-upstream-split && \
pytest -q tests/test_version.py::test_release_version_matches_v0191 \
tests/test_dependency_versions.py'
```

Expected:

- `tests/test_version.py::test_release_version_matches_v0191` 失败，实际值还是 `0.19.0`
- `tests/test_dependency_versions.py` 失败，指出 `requirements/common.txt` 仍是 `< 5`，`requirements/test.in` / `requirements/test.txt` 仍是 `4.57.5`

- [ ] **Step 3: 最小修改版本号与 requirements，并重新生成 `requirements/test.txt`**

`setup.py` 中将默认版本改为：

```python
DEFAULT_VLLM_RELEASE_VERSION = "0.19.1"
```

`requirements/common.txt` 中将 `transformers` 行替换为：

```text
transformers >= 4.56.0, != 5.0.*, != 5.1.*, != 5.2.*, != 5.3.*, != 5.4.*, != 5.5.0
```

并确认保留：

```text
compressed-tensors == 0.15.0.1
```

`requirements/test.in` 中将两行固定为：

```text
transformers==5.5.4
tokenizers==0.22.2
```

然后重新生成测试锁文件：

```bash
bash -lc 'cd /mnt/data/apps/1Cat-vLLM/.worktrees/vllm-0190-upstream-split && \
uv pip compile requirements/test.in -o requirements/test.txt \
--index-strategy unsafe-best-match \
--torch-backend cu129 \
--python-platform x86_64-manylinux_2_28 \
--python-version 3.12'
```

Expected:

- `requirements/test.txt` 中出现 `transformers==5.5.4`
- `requirements/test.txt` 中保留 `tokenizers==0.22.2`

- [ ] **Step 4: 复跑版本与依赖测试，确认从红转绿**

Run:

```bash
bash -lc 'source /home/z/anaconda3/etc/profile.d/conda.sh && \
conda activate gptq && \
cd /mnt/data/apps/1Cat-vLLM/.worktrees/vllm-0190-upstream-split && \
python setup.py --version && \
pytest -q tests/test_version.py tests/test_dependency_versions.py'
```

Expected:

- `python setup.py --version` 输出精确 `0.19.1`
- `tests/test_version.py` 通过
- `tests/test_dependency_versions.py` 通过

- [ ] **Step 5: 提交版本和 requirements 基线变更**

```bash
git -C /mnt/data/apps/1Cat-vLLM/.worktrees/vllm-0190-upstream-split add \
  setup.py \
  requirements/common.txt \
  requirements/test.in \
  requirements/test.txt \
  tests/test_version.py \
  tests/test_dependency_versions.py
git -C /mnt/data/apps/1Cat-vLLM/.worktrees/vllm-0190-upstream-split commit -m \
  "build: upgrade baseline to v0.19.1 and transformers 5.5.4"
```

## Task 2: 补齐 `huggingface_hub 1.x` 导入兼容

**Files:**
- Modify: `vllm/model_executor/model_loader/weight_utils.py`
- Modify: `tests/model_executor/test_weight_utils.py`
- Test: `tests/model_executor/test_weight_utils.py`

- [ ] **Step 1: 先加失败回归，覆盖旧常量不存在的场景**

在 `tests/model_executor/test_weight_utils.py` 里新增导入：

```python
import sys
from unittest.mock import MagicMock
```

把 `weight_utils` 的导入扩展成：

```python
from vllm.model_executor.model_loader.weight_utils import (
    download_weights_from_hf,
    enable_hf_transfer,
    enable_xet_high_performance,
    maybe_remap_kv_scale_name,
)
```

追加两个回归测试：

```python
def test_hf_transfer_auto_activation_is_noop_without_legacy_constant(monkeypatch):
    monkeypatch.delenv("HF_HUB_ENABLE_HF_TRANSFER", raising=False)
    monkeypatch.delattr(
        huggingface_hub.constants, "HF_HUB_ENABLE_HF_TRANSFER", raising=False
    )
    monkeypatch.setitem(sys.modules, "hf_transfer", MagicMock())

    enable_hf_transfer()

    assert not hasattr(huggingface_hub.constants, "HF_HUB_ENABLE_HF_TRANSFER")


def test_xet_high_performance_is_guarded_when_constant_is_missing(monkeypatch):
    monkeypatch.delenv("HF_XET_HIGH_PERFORMANCE", raising=False)
    monkeypatch.delattr(
        huggingface_hub.constants, "HF_XET_HIGH_PERFORMANCE", raising=False
    )

    enable_xet_high_performance()

    assert not hasattr(huggingface_hub.constants, "HF_XET_HIGH_PERFORMANCE")
```

并把现有 `test_hf_transfer_auto_activation()` 的最后一行改成：

```python
    if hasattr(huggingface_hub.constants, "HF_HUB_ENABLE_HF_TRANSFER"):
        assert huggingface_hub.constants.HF_HUB_ENABLE_HF_TRANSFER == HF_TRANSFER_ACTIVE
```

- [ ] **Step 2: 先跑单测，确认当前实现会在缺失旧常量时炸掉**

Run:

```bash
bash -lc 'source /home/z/anaconda3/etc/profile.d/conda.sh && \
conda activate gptq && \
cd /mnt/data/apps/1Cat-vLLM/.worktrees/vllm-0190-upstream-split && \
pytest -q tests/model_executor/test_weight_utils.py::test_hf_transfer_auto_activation_is_noop_without_legacy_constant \
tests/model_executor/test_weight_utils.py::test_xet_high_performance_is_guarded_when_constant_is_missing'
```

Expected:

- 至少一条测试失败
- 失败信息应直接指向 `AttributeError`，来源是给 `huggingface_hub.constants` 写入不存在的旧常量

- [ ] **Step 3: 最小修改 `weight_utils.py`，让旧常量缺失时安全退化**

将 `enable_hf_transfer()` 和 `enable_xet_high_performance()` 改成：

```python
def enable_hf_transfer():
    """automatically activates hf_transfer"""
    if "HF_HUB_ENABLE_HF_TRANSFER" not in os.environ:
        try:
            # enable hf hub transfer if available
            import hf_transfer  # type: ignore # noqa

            if hasattr(huggingface_hub.constants, "HF_HUB_ENABLE_HF_TRANSFER"):
                huggingface_hub.constants.HF_HUB_ENABLE_HF_TRANSFER = True
        except ImportError:
            pass


def enable_xet_high_performance():
    """automatically activates xet high performance mode"""
    if (
        "HF_XET_HIGH_PERFORMANCE" not in os.environ
        and hasattr(huggingface_hub.constants, "HF_XET_HIGH_PERFORMANCE")
    ):
        huggingface_hub.constants.HF_XET_HIGH_PERFORMANCE = True
```

- [ ] **Step 4: 复跑 `weight_utils` 测试，确认兼容防线成立**

Run:

```bash
bash -lc 'source /home/z/anaconda3/etc/profile.d/conda.sh && \
conda activate gptq && \
cd /mnt/data/apps/1Cat-vLLM/.worktrees/vllm-0190-upstream-split && \
pytest -q tests/model_executor/test_weight_utils.py'
```

Expected:

- `tests/model_executor/test_weight_utils.py` 通过
- 不再出现指向 `HF_HUB_ENABLE_HF_TRANSFER` 或 `HF_XET_HIGH_PERFORMANCE` 的 `AttributeError`

- [ ] **Step 5: 提交 Hub 兼容修复**

```bash
git -C /mnt/data/apps/1Cat-vLLM/.worktrees/vllm-0190-upstream-split add \
  vllm/model_executor/model_loader/weight_utils.py \
  tests/model_executor/test_weight_utils.py
git -C /mnt/data/apps/1Cat-vLLM/.worktrees/vllm-0190-upstream-split commit -m \
  "fix: guard huggingface hub v1 compatibility"
```

## Task 3: 增加本地 `Gemma4` tokenizer / processor 回归

**Files:**
- Create: `tests/tokenizers_/test_gemma4_local.py`
- Create: `tests/transformers_utils/test_gemma4_local_processor.py`
- Test: `tests/tokenizers_/test_gemma4_local.py`
- Test: `tests/transformers_utils/test_gemma4_local_processor.py`

- [ ] **Step 1: 新增本地 `Gemma4` tokenizer 回归**

创建 `tests/tokenizers_/test_gemma4_local.py`：

```python
# SPDX-License-Identifier: Apache-2.0
# SPDX-FileCopyrightText: Copyright contributors to the vLLM project

from pathlib import Path

import pytest
from transformers import AutoTokenizer

from vllm.tokenizers.hf import CachedHfTokenizer


GEMMA4_LOCAL = Path("/mnt/data6/models/gemma-4-31B-it-AWQ-4bit")
pytestmark = pytest.mark.skipif(
    not GEMMA4_LOCAL.exists(),
    reason="local Gemma4 model is not available",
)


def test_local_gemma4_auto_tokenizer_loads():
    tokenizer = AutoTokenizer.from_pretrained(
        str(GEMMA4_LOCAL),
        trust_remote_code=True,
    )
    assert tokenizer.__class__.__name__.startswith("Gemma")
    assert "<|video|>" in tokenizer.all_special_tokens


def test_local_gemma4_cached_tokenizer_loads():
    tokenizer = CachedHfTokenizer.from_pretrained(
        str(GEMMA4_LOCAL),
        trust_remote_code=True,
    )
    token_ids = tokenizer.encode("hello from gemma4")
    assert isinstance(token_ids, list)
    assert len(token_ids) > 0
```

- [ ] **Step 2: 新增本地 `Gemma4` processor 回归**

创建 `tests/transformers_utils/test_gemma4_local_processor.py`：

```python
# SPDX-License-Identifier: Apache-2.0
# SPDX-FileCopyrightText: Copyright contributors to the vLLM project

from pathlib import Path

import pytest

from vllm.transformers_utils.processor import get_processor


GEMMA4_LOCAL = Path("/mnt/data6/models/gemma-4-31B-it-AWQ-4bit")
pytestmark = pytest.mark.skipif(
    not GEMMA4_LOCAL.exists(),
    reason="local Gemma4 model is not available",
)


def test_local_gemma4_processor_loads():
    processor = get_processor(
        str(GEMMA4_LOCAL),
        trust_remote_code=True,
    )
    assert getattr(processor, "tokenizer", None) is not None
    assert processor.tokenizer.__class__.__name__.startswith("Gemma")
```

- [ ] **Step 3: 在 `gptq` 环境安装目标依赖，并跑红绿测试**

Run:

```bash
bash -lc 'source /home/z/anaconda3/etc/profile.d/conda.sh && \
conda activate gptq && \
cd /mnt/data/apps/1Cat-vLLM/.worktrees/vllm-0190-upstream-split && \
python -m pip install --upgrade \
  "huggingface_hub>=1.5,<2" \
  "transformers==5.5.4" \
  "tokenizers==0.22.2" && \
pytest -q tests/tokenizers_/test_gemma4_local.py \
tests/transformers_utils/test_gemma4_local_processor.py'
```

Expected:

- 在旧 `4.57.6` 环境下这两组测试会因为 tokenizer / processor 初始化失败而红
- 升到 `5.5.4` 后，两组测试都通过

- [ ] **Step 4: 重新安装 editable 包，确保运行时代码和新依赖对齐**

Run:

```bash
bash -lc 'source /home/z/anaconda3/etc/profile.d/conda.sh && \
conda activate gptq && \
cd /mnt/data/apps/1Cat-vLLM/.worktrees/vllm-0190-upstream-split && \
python -m pip install -e . --no-build-isolation'
```

Expected:

- editable install 成功
- 后续 `pytest` 和 `serve` 都从当前 worktree 导入，而不是旧的 site-packages 副本

- [ ] **Step 5: 提交 `Gemma4` tokenizer / processor 回归测试**

```bash
git -C /mnt/data/apps/1Cat-vLLM/.worktrees/vllm-0190-upstream-split add \
  tests/tokenizers_/test_gemma4_local.py \
  tests/transformers_utils/test_gemma4_local_processor.py
git -C /mnt/data/apps/1Cat-vLLM/.worktrees/vllm-0190-upstream-split commit -m \
  "test: add local gemma4 tokenizer and processor regressions"
```

## Task 4: 保留 `SM70` 的 `FlashAttention / Gemma4` 混合 attention 路径

**Files:**
- Preserve and verify: `vllm/v1/attention/backends/flash_attn.py`
- Preserve and verify: `vllm/model_executor/models/config.py`
- Preserve and verify: `vllm/model_executor/models/gemma4.py`
- Preserve and verify: `tests/v1/attention/test_flash_attn_sm70.py`
- Preserve and verify: `tests/v1/attention/test_gemma4_sm70_mixed_attention.py`
- Test: `tests/v1/attention/test_flash_attn_sm70.py`
- Test: `tests/v1/attention/test_gemma4_sm70_mixed_attention.py`

- [ ] **Step 1: 确认 `SM70` attention 回归测试已经在 worktree 中，必要时补齐**

`tests/v1/attention/test_flash_attn_sm70.py` 应至少包含：

```python
def test_flash_attention_sm70_supports_hdim512():
    assert _validate_flash_attn(512, DeviceCapability(7, 0)) == []


def test_flash_attention_hdim512_is_sm70_specific():
    reasons = _validate_flash_attn(512, DeviceCapability(8, 0))
    assert any("head_size > 256" in reason for reason in reasons)
```

`tests/v1/attention/test_gemma4_sm70_mixed_attention.py` 应至少包含：

```python
def test_gemma4_sm70_full_hdim512_layers_prefer_flash_attn() -> None:
    backend = _select_gemma4_text_attention_backend(
        layer_type="full_attention",
        head_dim=512,
        capability=DeviceCapability(7, 0),
        user_backend=None,
        kv_transfer_enabled=False,
    )
    assert backend is FlashAttentionBackend


def test_gemma4_mixed_attention_is_sm70_only() -> None:
    backend = _select_gemma4_text_attention_backend(
        layer_type="full_attention",
        head_dim=512,
        capability=DeviceCapability(8, 0),
        user_backend=None,
        kv_transfer_enabled=False,
    )
    assert backend is None
```

- [ ] **Step 2: 先跑注意力测试，检查依赖升级有没有把本地 patch 冲掉**

Run:

```bash
bash -lc 'source /home/z/anaconda3/etc/profile.d/conda.sh && \
conda activate gptq && \
cd /mnt/data/apps/1Cat-vLLM/.worktrees/vllm-0190-upstream-split && \
pytest -q tests/v1/attention/test_flash_attn_sm70.py \
tests/v1/attention/test_gemma4_sm70_mixed_attention.py'
```

Expected:

- 如果当前 worktree 里这些本地 patch 还在，测试直接通过
- 如果版本同步时被覆盖，测试会直接指向 `head_dim=512` 或 `Gemma4` layer backend 选择失败

- [ ] **Step 3: 如果测试失败，只恢复这三处关键实现，不要把文件整份覆盖回旧版**

`vllm/v1/attention/backends/flash_attn.py` 中关键逻辑应为：

```python
@classmethod
def supports_head_size(cls, head_size: int) -> bool:
    return head_size % 8 == 0 and head_size <= 512


@classmethod
def supports_combination(
    cls,
    head_size: int,
    dtype: torch.dtype,
    kv_cache_dtype: CacheDType | None,
    block_size: int | None,
    use_mla: bool,
    has_sink: bool,
    use_sparse: bool,
    device_capability: DeviceCapability,
) -> str | None:
    if head_size > 256 and device_capability != DeviceCapability(7, 0):
        return "head_size > 256 is only supported by SM70 FlashAttention kernels"
    if has_sink and device_capability < DeviceCapability(9, 0):
        return "sink not supported on compute capability < 9.0"
    return None
```

`vllm/model_executor/models/config.py` 中 `Gemma4Config` 应保持：

```python
class Gemma4Config(VerifyAndUpdateConfig):
    @staticmethod
    def verify_and_update_config(vllm_config: "VllmConfig") -> None:
        # Gemma4 resolves heterogeneous attention backends at model-layer
        # construction time in gemma4.py. Keep the global backend unset so
        # per-layer overrides can express the SM70 strategy.
        return
```

`vllm/model_executor/models/gemma4.py` 中应保留：

```python
def _select_gemma4_text_attention_backend(
    *,
    layer_type: str,
    head_dim: int,
    capability: DeviceCapability | object | None,
    user_backend: AttentionBackendEnum | None,
    kv_transfer_enabled: bool,
) -> type[AttentionBackend] | None:
    def is_sm70(capability: DeviceCapability | object | None) -> bool:
        if capability is None:
            return False
        if isinstance(capability, DeviceCapability):
            return capability == DeviceCapability(7, 0)
        to_int = getattr(capability, "to_int", None)
        return callable(to_int) and to_int() == 70

    if user_backend is not None:
        return None
    if kv_transfer_enabled:
        return None
    if not is_sm70(capability):
        return None

    if layer_type in ("sliding_attention", "full_attention"):
        if FlashAttentionBackend.supports_head_size(head_dim):
            return FlashAttentionBackend
    return None
```

并在 `Gemma4Attention.__init__()` 里保留：

```python
layer_backend = _select_gemma4_text_attention_backend(
    layer_type=layer_type,
    head_dim=self.head_dim,
    capability=current_platform.get_device_capability(),
    user_backend=vllm_config.attention_config.backend,
    kv_transfer_enabled=_gemma4_kv_transfer_enabled(vllm_config),
)
```

- [ ] **Step 4: 复跑 attention 单测，确认 `SM70 hdim512` 仍然成立**

Run:

```bash
bash -lc 'source /home/z/anaconda3/etc/profile.d/conda.sh && \
conda activate gptq && \
cd /mnt/data/apps/1Cat-vLLM/.worktrees/vllm-0190-upstream-split && \
pytest -q tests/v1/attention/test_flash_attn_sm70.py \
tests/v1/attention/test_gemma4_sm70_mixed_attention.py'
```

Expected:

- 两个测试文件全部通过
- 不再出现 `head_size <= 256` 或 `backend is None` 的失败

- [ ] **Step 5: 提交 `SM70` attention / Gemma4 保留改动**

```bash
git -C /mnt/data/apps/1Cat-vLLM/.worktrees/vllm-0190-upstream-split add \
  vllm/v1/attention/backends/flash_attn.py \
  vllm/model_executor/models/config.py \
  vllm/model_executor/models/gemma4.py \
  tests/v1/attention/test_flash_attn_sm70.py \
  tests/v1/attention/test_gemma4_sm70_mixed_attention.py
git -C /mnt/data/apps/1Cat-vLLM/.worktrees/vllm-0190-upstream-split commit -m \
  "feat: preserve sm70 gemma4 flash attention routing"
```

## Task 5: 保留 `compressed-tensors` 的 `SM70` dense AWQ 路径

**Files:**
- Preserve and verify: `vllm/model_executor/layers/quantization/compressed_tensors/compressed_tensors.py`
- Preserve and verify: `vllm/model_executor/layers/quantization/compressed_tensors/schemes/__init__.py`
- Preserve and verify: `vllm/model_executor/layers/quantization/compressed_tensors/schemes/compressed_tensors_wNa16_sm70.py`
- Preserve and verify: `tests/quantization/test_compressed_tensors_sm70.py`
- Test: `tests/quantization/test_compressed_tensors_sm70.py`

- [ ] **Step 1: 确认 `compressed-tensors` 的 `SM70` 回归测试已经在 worktree 中**

`tests/quantization/test_compressed_tensors_sm70.py` 需要至少覆盖：

Run:

```bash
cd /mnt/data/apps/1Cat-vLLM/.worktrees/vllm-0190-upstream-split
rg -n 'test_ct_dense_sm70_selects_sm70_scheme|test_ct_dense_sm70_incompatible_layer_uses_internal_fallback|test_ct_dense_sm70_apply_uses_awq_gemm_sm70' \
  tests/quantization/test_compressed_tensors_sm70.py
rg -n 'CompressedTensorsSM70WNA16|torch.all\\(out == 4\\)' \
  tests/quantization/test_compressed_tensors_sm70.py
```

Expected:

- 三个测试函数都存在
- 文件里存在 `CompressedTensorsSM70WNA16` 和 `torch.all(out == 4)` 断言

- [ ] **Step 2: 先跑量化单测，确认依赖升级没有把本地 scheme 选择冲掉**

Run:

```bash
bash -lc 'source /home/z/anaconda3/etc/profile.d/conda.sh && \
conda activate gptq && \
cd /mnt/data/apps/1Cat-vLLM/.worktrees/vllm-0190-upstream-split && \
pytest -q tests/quantization/test_compressed_tensors_sm70.py'
```

Expected:

- 如果当前 worktree 中的本地实现还在，测试直接通过
- 如果版本同步时把 `SM70` 选择逻辑覆盖掉，测试会在 scheme 类型或 `awq_gemm_sm70` 路径上失败

- [ ] **Step 3: 如果测试失败，只恢复当前 worktree 已经验证过的两个关键块**

`vllm/model_executor/layers/quantization/compressed_tensors/compressed_tensors.py` 中的 `get_scheme()` 选择块应为：

```python
if (
    self._is_wNa16_group_channel(weight_quant, input_quant)
    and (format == CompressionFormat.pack_quantized.value)
    and (weight_quant.num_bits in WNA16_SUPPORTED_BITS)
):
    capability = current_platform.get_device_capability()
    if (
        capability is not None
        and capability.to_int() == 70
        and weight_quant.num_bits == 4
        and weight_quant.symmetric
        and weight_quant.group_size in (32, 64, 128)
        and weight_quant.actorder != ActivationOrdering.GROUP
    ):
        return CompressedTensorsSM70WNA16(
            num_bits=weight_quant.num_bits,
            strategy=weight_quant.strategy,
            symmetric=weight_quant.symmetric,
            group_size=weight_quant.group_size,
            actorder=weight_quant.actorder,
            layer_name=layer_name,
        )
    return CompressedTensorsWNA16(
        num_bits=weight_quant.num_bits,
        strategy=weight_quant.strategy,
        symmetric=weight_quant.symmetric,
        group_size=weight_quant.group_size,
        actorder=weight_quant.actorder,
        layer_name=layer_name,
    )
```

`vllm/model_executor/layers/quantization/compressed_tensors/schemes/__init__.py` 中应保留：

```python
from .compressed_tensors_wNa16_sm70 import CompressedTensorsSM70WNA16

__all__ = [
    "CompressedTensorsScheme",
    "CompressedTensorsWNA16",
    "CompressedTensorsSM70WNA16",
    "CompressedTensorsW8A16Fp8",
    "CompressedTensorsW8A8Int8",
    "CompressedTensorsW8A8Fp8",
    "WNA16_SUPPORTED_BITS",
    "CompressedTensors24",
    "CompressedTensorsW4A16Fp4",
    "CompressedTensorsW4A16Mxfp4",
    "CompressedTensorsW4A4Fp4",
    "CompressedTensorsW4A8Int",
    "CompressedTensorsW4A8Fp8",
]
```

`vllm/model_executor/layers/quantization/compressed_tensors/schemes/compressed_tensors_wNa16_sm70.py` 中的核心路径应保留：

```python
class CompressedTensorsSM70WNA16(CompressedTensorsWNA16):
    """SM70 dense int4 path backed by the TurboMind AWQ kernels."""

    @classmethod
    def get_min_capability(cls) -> int:
        return 70

    def process_weights_after_loading(self, layer: torch.nn.Module) -> None:
        if not self._sm70_compatible:
            self._sm70_compatible = self._infer_sm70_compatibility(layer)

        if not self._sm70_compatible or not hasattr(ops, "awq_sm70_prepare"):
            return

        qweight = self._ct_to_awq_qweight(layer.weight_packed.data)
        scales = layer.weight_scale.data.transpose(0, 1).contiguous().to(torch.float16)
        qzeros = self._make_symmetric_awq_qzeros(
            num_groups=scales.shape[0],
            output_size_per_partition=layer.weight_packed.shape[0],
            device=qweight.device,
        )
        tm_weight, tm_scales, meta = ops.awq_sm70_prepare(
            qweight,
            scales,
            qzeros,
            self.group_size,
        )
        layer._awq_sm70_weight = tm_weight
        layer._awq_sm70_scales = tm_scales
        layer._awq_sm70_k_ld = int(meta[0])
        layer._awq_sm70_q_ld = int(meta[1])
        layer._awq_sm70_prepared = True

    def apply_weights(
        self, layer: torch.nn.Module, x: torch.Tensor, bias: torch.Tensor | None
    ) -> torch.Tensor:
        if getattr(layer, "_awq_sm70_prepared", False) and hasattr(ops, "awq_gemm_sm70"):
            out = ops.awq_gemm_sm70(
                x.reshape(-1, x.shape[-1]),
                layer._awq_sm70_weight,
                layer._awq_sm70_scales,
                self.group_size,
                layer._awq_sm70_k_ld,
                layer._awq_sm70_q_ld,
            )
            out = out.reshape(*x.shape[:-1], out.shape[-1])
            if bias is not None:
                out = out + bias
            return out
        return self._apply_ct_fallback(layer, x, bias)
```

- [ ] **Step 4: 复跑量化单测，确认 `SM70 dense AWQ` 仍可用**

Run:

```bash
bash -lc 'source /home/z/anaconda3/etc/profile.d/conda.sh && \
conda activate gptq && \
cd /mnt/data/apps/1Cat-vLLM/.worktrees/vllm-0190-upstream-split && \
pytest -q tests/quantization/test_compressed_tensors_sm70.py'
```

Expected:

- `tests/quantization/test_compressed_tensors_sm70.py` 全部通过
- 不再出现 `CompressedTensorsWNA16` 误选、`awq_sm70_prepare` 未走到、或 `awq_gemm_sm70` 未调用的失败

- [ ] **Step 5: 提交 `compressed-tensors` 的 `SM70` 保留改动**

```bash
git -C /mnt/data/apps/1Cat-vLLM/.worktrees/vllm-0190-upstream-split add \
  vllm/model_executor/layers/quantization/compressed_tensors/compressed_tensors.py \
  vllm/model_executor/layers/quantization/compressed_tensors/schemes/__init__.py \
  vllm/model_executor/layers/quantization/compressed_tensors/schemes/compressed_tensors_wNa16_sm70.py \
  tests/quantization/test_compressed_tensors_sm70.py
git -C /mnt/data/apps/1Cat-vLLM/.worktrees/vllm-0190-upstream-split commit -m \
  "feat: preserve sm70 compressed tensors dense awq path"
```

## Task 6: 更新文档并完成真实 `Gemma4` 验证

**Files:**
- Modify: `README.md`
- Modify: `docs/open_source_sm70_flash_attn_install_upgrade_zh.md`
- Test: `tests/test_version.py`
- Test: `tests/test_dependency_versions.py`
- Test: `tests/model_executor/test_weight_utils.py`
- Test: `tests/tokenizers_/test_gemma4_local.py`
- Test: `tests/transformers_utils/test_gemma4_local_processor.py`
- Test: `tests/v1/attention/test_flash_attn_sm70.py`
- Test: `tests/v1/attention/test_gemma4_sm70_mixed_attention.py`
- Test: `tests/quantization/test_compressed_tensors_sm70.py`

- [ ] **Step 1: 更新 README 和安装文档中的版本口径**

`README.md` 顶部和版本说明改成：

```markdown
# v100llm (vLLM 0.19.1 for V100/SM70)

`v100llm` 是一个面向 `Tesla V100 / SM70` 的 `vLLM 0.19.1` 定向分支。这个仓库的重点不是重新定义
`vllm` 的 Python API，而是在保持现有 `vllm` 包和 CLI 习惯不变的前提下，恢复并验证 V100 上真实需要的
推理路径，包括 `FLASH_ATTN`、`AWQ`、`compressed-tensors MoE` 和 MiniMax/Qwen3.5 相关运行时链路。

- 基于 upstream `vLLM 0.19.1`，但面向 `V100 / SM70` 做了运行时恢复与兼容性补充。
- 当前默认对外打包版本固定为 `0.19.1`；`python setup.py --version`、`vllm.__version__` 和 CLI `--version` 已对齐。
- 当前 `Gemma4` 相关验证基线使用 `transformers 5.5.4`。
```

README 中的期望输出改成：

```text
0.19.1
```

`docs/open_source_sm70_flash_attn_install_upgrade_zh.md` 的“当前推荐环境”补成：

```markdown
下面这组版本是当前仓库已经实际验证过的组合：

- Python：`3.13`
- PyTorch：`2.10.0+cu128`
- Transformers：`5.5.4`
- Tokenizers：`0.22.2`
- Triton：`3.6.0`
- CUDA toolkit：`12.8`
```

并在安装 `1Cat-vLLM` 后追加：

```bash
python -m pip install --upgrade "transformers==5.5.4" "tokenizers==0.22.2" "huggingface_hub>=1.5,<2"
```

- [ ] **Step 2: 跑完整的本地回归测试矩阵**

Run:

```bash
bash -lc 'source /home/z/anaconda3/etc/profile.d/conda.sh && \
conda activate gptq && \
cd /mnt/data/apps/1Cat-vLLM/.worktrees/vllm-0190-upstream-split && \
pytest -q \
  tests/test_version.py \
  tests/test_dependency_versions.py \
  tests/model_executor/test_weight_utils.py \
  tests/tokenizers_/test_gemma4_local.py \
  tests/transformers_utils/test_gemma4_local_processor.py \
  tests/v1/attention/test_flash_attn_sm70.py \
  tests/v1/attention/test_gemma4_sm70_mixed_attention.py \
  tests/quantization/test_compressed_tensors_sm70.py'
```

Expected:

- 上述测试全部通过
- 没有新的 `transformers 5.5.4` 导入错误

- [ ] **Step 3: 跑真实 `Gemma4` 2-GPU offline smoke**

Run:

```bash
bash -lc 'source /home/z/anaconda3/etc/profile.d/conda.sh && \
conda activate gptq && \
cd /mnt/data/apps/1Cat-vLLM/.worktrees/vllm-0190-upstream-split && \
CUDA_DEVICE_ORDER=PCI_BUS_ID CUDA_VISIBLE_DEVICES=2,3 python - <<\"PY\"\n\
from vllm import LLM, SamplingParams\n\
\n\
model = \"/mnt/data6/models/gemma-4-31B-it-AWQ-4bit\"\n\
llm = LLM(\n\
    model=model,\n\
    tensor_parallel_size=2,\n\
    dtype=\"float16\",\n\
    quantization=\"compressed-tensors\",\n\
    max_model_len=4096,\n\
)\n\
out = llm.generate([\"Explain why cache stampede hurts latency.\"], SamplingParams(max_tokens=64, temperature=0))\n\
print(out[0].outputs[0].finish_reason)\n\
print(out[0].outputs[0].text[:200])\n\
PY'
```

Expected:

- 模型成功加载
- 至少返回一条生成结果
- `finish_reason` 为 `length` 或 `stop`
- 输出文本不是空串

- [ ] **Step 4: 跑真实 `serve / OpenAI API` smoke，确认不再需要 `--skip-tokenizer-init`**

Run the server in one shell:

```bash
bash -lc 'source /home/z/anaconda3/etc/profile.d/conda.sh && \
conda activate gptq && \
cd /mnt/data/apps/1Cat-vLLM/.worktrees/vllm-0190-upstream-split && \
CUDA_DEVICE_ORDER=PCI_BUS_ID CUDA_VISIBLE_DEVICES=2,3 \
python -m vllm.entrypoints.openai.api_server \
  --model /mnt/data6/models/gemma-4-31B-it-AWQ-4bit \
  --quantization compressed-tensors \
  --dtype float16 \
  --tensor-parallel-size 2 \
  --gpu-memory-utilization 0.94 \
  --max-model-len 33024 \
  --port 24170'
```

Wait for `/v1/models` to come up, then in another shell run:

```bash
curl -sS http://127.0.0.1:24170/v1/chat/completions \
  -H 'Content-Type: application/json' \
  -d '{
    "model": "/mnt/data6/models/gemma-4-31B-it-AWQ-4bit",
    "messages": [{"role": "user", "content": "用一句话解释缓存雪崩。"}],
    "temperature": 0,
    "max_tokens": 64
  }'
```

Expected:

- 服务能在不加 `--skip-tokenizer-init` 的前提下启动
- 响应里 `choices[0].finish_reason` 为 `stop` 或 `length`
- 响应里 `choices[0].message.content` 是可读文本，而不是只能靠 token ids 回解

- [ ] **Step 5: 提交文档与最终验证改动**

```bash
git -C /mnt/data/apps/1Cat-vLLM/.worktrees/vllm-0190-upstream-split add \
  README.md \
  docs/open_source_sm70_flash_attn_install_upgrade_zh.md
git -C /mnt/data/apps/1Cat-vLLM/.worktrees/vllm-0190-upstream-split commit -m \
  "docs: update v0.19.1 and transformers 5.5.4 guidance"
```

- [ ] **Step 6: 记录最终验收命令，作为分支收尾口径**

Run:

```bash
bash -lc 'source /home/z/anaconda3/etc/profile.d/conda.sh && \
conda activate gptq && \
cd /mnt/data/apps/1Cat-vLLM/.worktrees/vllm-0190-upstream-split && \
python setup.py --version && \
python -c "import transformers, vllm; print(transformers.__version__); print(vllm.__version__)"'
```

Expected:

- 输出 `0.19.1`
- 输出 `5.5.4`
- 输出 `0.19.1`
